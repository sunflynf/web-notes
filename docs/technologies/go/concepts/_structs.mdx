## Structs & Methods

```go
// Structs are used to group related data together
type Person struct {
    Name string
    Age  int
}

// Using
func (p Person) Greet() {
    fmt.Println("Hello, my name is", p.Name)
}
```